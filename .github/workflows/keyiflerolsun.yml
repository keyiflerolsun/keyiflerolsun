# Bu araç @keyiflerolsun tarafından | @KekikAkademi için yazılmıştır.

name: Otomatik Profil Güncelleyici

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

on: 
  workflow_dispatch:
  schedule:
    - cron: 00 00 * * *
  push:
    paths:
      - guncelle.py

jobs:
  keyiflerolsun:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name : Depo Kontrolü
        uses : actions/checkout@v4

      - name : Python 3.11.8 Yükle
        uses : actions/setup-python@v5
        with :
          python-version: 3.11.8

      - name : Python Sürümünü Göster
        run  : python -c "import sys; print(sys.version)"

      - name : Gereksinimleri Yükle
        run  : |
          python -m pip install --upgrade pip
          pip install -Ur requirements.txt

      - name : Betiği Çalıştır
        run  : |
          python guncelle.py

      - name : Değişiklik Kontrolü
        id   : degisiklik_kontrol
        run  : |
          git diff --quiet . || echo "degisiklik=true" >> $GITHUB_OUTPUT

      - name : Depoyu Güncelle
        if   : steps.degisiklik_kontrol.outputs.degisiklik == 'true'
        uses : stefanzweifel/git-auto-commit-action@v5
        with :
          commit_author  : keyiflerolsun <keyiflerolsun@users.noreply.github.com>
          commit_message : 🕰 Otomatik Paylaşım » $(TZ="Europe/Istanbul" date "+%d-%m-%Y %H:%M:%S | TSİ") 💾 Önceki » $GITHUB_SHA
